const carModel = {
  manufacturer: "VW",
  modell: "Golf",
};

const entriesArray = Object.entries(carModel);
console.log(entriesArray);

const keysArray = Object.keys(carModel);
console.log(keysArray);

const valuesArray = Object.values(carModel);
console.log(valuesArray);
